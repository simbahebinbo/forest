window.SIDEBAR_ITEMS = {"struct":[["ActorState","Newtype to wrap different versions of `fvm::state_tree::ActorState`"],["StateTree","Re-use `StateTree` from FVM2 directly, without wrapping. Moving forward, we should use `StateTree` from FVM3. Unfortunately, for the time being, we are blocked by lack of migrations. State tree implementation using hamt. This structure is not threadsafe and should only be used in sync contexts."]]};